// Short Syntax
const name = "Thiago";
const age = 26;

// Caso o nome da propriedade seja o mesmo da variável podemos escrever da seguinte forma
const user = {
    name,
    age
}

console.log(user);